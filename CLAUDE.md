@AGENTS.md

Read `README.md` "Picking this up" first. `docs/roadmap.md` is the decision
log. Skills: `sink-check` for the browser gates, `rux-ds-page` for building
a page.

Work an app needs from this system — a class, a rule, an icon, a template
fix — is done here directly, in the same session as the app change, with
invented content (`docs/consumer-policy.md`). No memo is written and nothing
waits on a reply. What is still unfinished across the family is one list:
`../rux-sm.github.io/docs/status.md`.
